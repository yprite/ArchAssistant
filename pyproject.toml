[project]
name = "archanalyzer"
version = "0.1.0"
description = "DDD architecture analyzer and viewer"
requires-python = ">=3.10"
dependencies = ["PySide6>=6.5"]

[tool.setuptools]
packages = ["analyzer", "core", "ui"]
